1. 'edges-master' folder contains a 'readme.txt', which contains the environment setup
2. the main function is 'draw_heatmap.m'. only one line needs to be changed:
	a) line 23, 'filename' should points to the image file of everyframe

